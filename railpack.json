{
  "build": {
    "buildCommand": "cargo build --release -p pointercrate-example && cp target/release/pointercrate-example bin/pointercrate-example"
  },
  "deploy": {
    "startCommand": "./bin/pointercrate-example"
  }
}
